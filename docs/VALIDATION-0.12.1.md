# 0.12.1 验证记录

已完成：

- 前端测试 73 项全部通过，含新增 19 项标点、空白、段落、Ruby 与原文保护测试。
- TypeScript / Vue 类型检查、标准版 Vite 和 WebKitGTK 4.0 目标 Vite 构建通过。
- 用户提供的完整“无论是好／是坏”文字用于回归测试；分别模拟短行字号 16/20、起点偏移一个字宽和较大行距，均连接句内折行并保留后续段落。
- 序号句点、小数、版本、中文域名、文件名、括号、省略号、时间、英文全角标点、重复空格、假名、代码和缩进测试包含重复整理幂等性检查。
- Ruby 起止偏移在删除空格与缩短省略号后仍绑定原来的汉字；原始内容未变，纯文本与 HTML 共同使用规范化后的正文。
- 表格模式和坐标缺失的旧页保持原文；既有跨页完整性、原始视图、手工校对保护测试继续通过。

验证限制：

- Python 共运行 93 项，88 项通过，5 项因本地缺少 cv2（OpenCV）失败；没有跳过或伪装通过。这些依赖由既有 Actions 安装，源码包不改变 Python 依赖版本。
- 尚未在本地构建 Windows NSIS / Linux AppImage、运行冻结 Paddle 模型或安装包；三平台交付须由 GitHub Actions 和目标电脑验证。
- 用户该次识别的真实坐标/字号未提供，因此短行问题是依据可复现的模拟版面修复，不能保证每一种实际版面都能判断正确。
- 本次未运行浏览器截图检查，不将前端生产构建等同于原生桌面视觉验收。

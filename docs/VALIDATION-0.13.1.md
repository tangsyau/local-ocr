# 0.13.1 验证记录

日志 logs_92188918959.zip：Windows 与 WebKitGTK 4.0 构建及上传成功；普通 Linux 在 test:layout 最后一列可达性断言失败。

修复：横向工具条与表格按实时可滚动范围比例双向同步，避免不同视口、滚动槽或尺寸变化造成末列不可达；观察工具条尺寸变化。保留 CI 末列断言，增加实际尺寸诊断。

本地：前端 87 项测试通过，npm run build 通过，Python 项目配置测试通过。新增回归覆盖不等滚动范围、尺寸变化、双向同步与左右端点。

本次浏览器布局检查未能在当前环境运行，不能声明其已通过；需由 GitHub Actions 继续验证。未在本地构建原生安装包。

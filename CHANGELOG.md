# 0.12.0

- 智能整理新增保守的中日文标点规范化、OCR 多余空格清理和连续空行压缩。
- 保留英文、小数、网址和邮箱中的半角标点；Ruby HTML 标签和实体不会被清洗规则破坏。
- 原始结果、整理前文本、手工校对和表格结果保持不变。

# 0.11.3

- 专注模式下隐藏普通结果区的“整理前 / 整理后”切换按钮，避免与双栏对照产生重复操作。
- 将 Ruby 预览选项改名为“显示日语注音（Ruby）”，悬停可查看关闭后的校对说明。
- 修正“应用到勾选的 PDF”按钮未继承页码控件字号的问题。

# 0.11.2

- 修复通用 `font: inherit` 覆盖文档设置下拉菜单字号的问题，文档设置与页码输入控件现在使用统一字号。

# 0.11.1

- 统一文档预览设置区的文字与下拉菜单字号，避免控件文字明显大于其他界面。
- 指定页码输入框始终显示；点击或输入页码会自动切换到“指定页码”。

# 0.11.0

- 页码选择改为“全部页 / 指定页码”单选项；指定页码输入框直接编辑，当前 PDF 自动作为默认作用范围。
- 批量页码按钮改为“应用到勾选的 PDF（N）”，显示勾选数量并明确批量作用范围。
- 识别结果版本改为单击切换的“整理前 / 整理后”标签。
- 专注模式的文本视图提供整理前后左右对照，并为两栏分别提供复制全文按钮；表格视图保持原有单栏模式。

# 0.10.1

- “文档设置”改为默认展开，首次打开即可看到 PDF 文字来源、文本整理、跨页正文、日语注音和注音输出。
- 五项设置统一为标题行与控件行结构；下拉框等宽等高，复选项使用相同高度和基线，按可用宽度自动换列。
- 未启用日语注音时禁用注音输出菜单，避免把输出格式误认为已经启用注音识别。
- 布局测试新增默认展开、下拉框等高及同行设置等高检查；不修改 OCR、文本整理或模型格式。

# 0.10.0

- PDF 文字模式新增自动取文/强制 OCR；逐页检查文本层，输出统一坐标与来源，保持原有暂停、取消及页级续识语义。
- 新增实验性横排/竖排日语注音分离：PDF 字符坐标绑定，扫描页小字候选检测、局部放大识别；保存原始数据，提供忽略、括号和 HTML Ruby 输出。
- 文本整理支持原始断行、智能与连续文本；中日英边界空格、保守段落判断、完整任务的相邻页正文连接、重复页眉页脚及页码过滤。表格不进入普通正文重排。
- 新增原始只读视图与手工校对保护；整理缓存按页复用，切换格式不重新推理；缺少版面信息的旧结果安全保留。
- 文字模式新增 HTML 导出，TXT/复制采用当前文本；HTML 仅允许安全的 Ruby/表格标签，禁止外部资源。
- 三平台原有 Actions 保留，增补冻结进程的文本层/混合页/Ruby 分支验证；模型版本和 glibc 兼容基线不变。

# 0.9.2

- 修复普通 Linux 布局检查：不再对已经升级到版本 2 的 `local-ocr-session` IndexedDB 执行版本 1 打开操作，避免浏览器抛出 `VersionError`。
- 旧会话迁移测试改为在版本 2 数据库中写入 schema 1 会话内容，仍完整验证“内联逐页结果 → schema 2 独立页面存储”的真实升级路径。
- IndexedDB 测试错误改为显式抛出请求或事务错误，避免 Playwright 只显示无法定位的 `page.evaluate: Event`。
- 本次日志确认 0.9.1 的 Windows NSIS 与 WebKitGTK 4.0 / glibc 2.28 AppImage 已通过真实模型、打包、基线检查及安装包启动测试；本版不修改 OCR 或应用界面。

# 0.9.1

- 修复普通 Linux 与 WebKitGTK 4.0 两项 GitHub Actions 中相同的 npm Arborist `edgesOut` 内部错误，移除 `npm ci` 后的两处 `npm install --no-save`。
- Playwright 1.62.1 纳入根 `package.json` 和锁文件，布局测试直接调用已锁定的本地命令，不再临时修改根依赖树。
- WebKitGTK 4.0 任务不再将 Tauri 1 CLI 临时加入已有 Tauri 2 的根依赖树。
- 为 Tauri CLI 1.6.3 增加独立的 `package.json` 和锁文件；兼容任务使用独立目录执行 `npm ci`，随后从明确路径构建 AppImage。
- Node 缓存键同时覆盖根前端锁文件与兼容 CLI 锁文件，并增加工作流回归检查，防止重新引入两个 Tauri CLI 主版本的依赖树冲突。
- 根据本次日志确认 Windows 0.9.0 的源码测试、冻结 sidecar、真实模型验证、NSIS 构建及安装启动检查均成功；普通 Linux 与兼容 Linux 均在 OCR 检查之后的临时 npm 安装步骤失败，不是新的 OCR、glibc 或 WebKitGTK 运行错误。
- 不修改 OCR、自动保存、断点续识或模型格式；继续使用 Debian 10、WebKitGTK 4.0 和 glibc 2.28 基线。

# 0.9.0

- 自动保存数据库升级为 schema 2：任务/设置摘要与逐页结果分开存储。新完成的一页只写入一个页记录，摘要不再重复保存整份 `pages` 和合并表格；schema 1 在首次保存时无损迁移。
- 逐页结果改为原位增量累计页数、文本块和原始表格，跨页表格只在任务结束时统一合并，避免长 PDF 每完成一页都重新遍历、复制和合并此前全部页面。
- 修复旧结果模式恢复：优先使用结果自身模式，并可依据旧表格内容推断表格模式；旧结果缺少逐页结构时会构造兼容页，重新识别当前文件时自动选回该结果的模型档位和模式。
- PDF 失败、取消、强制停止或程序异常退出后可按页续识。仅当已保存页面构成所选页码的连续前缀，且文件大小、修改时间、模型档位、识别模式、置信度、页码范围和旋转参数完全一致时复用；否则安全地从头识别。
- 断点续识向 sidecar 只发送已完成页号，不重复传输识别文字；进度包含此前完成页，返回结果标明本轮处理页数和累计完成页数。
- 添加文件时按自然文件名排序，并提供“自然排序”按钮；`page2` 排在 `page10` 之前。用户仍可在空闲时手动上移、下移任务。
- 增加旧模式迁移、分离式保存摘要、增量页聚合、断点安全条件、sidecar 跳页和自然排序回归测试；冻结包冒烟测试增加指定页码断点续识。
- 沿用 0.8.1 的 Windows 初始化与小窗口预览修复，不升级 Paddle 依赖，不改变 WebKitGTK 4.0 / glibc 2.28 构建基线；0.8.x 模型包仍可导入。

边界：断点粒度是一整页，正在推理的一页仍会在下次重新计算；手动点击“重新识别当前文件”表示主动替换旧结果，会从头开始。0.8.x 未记录来源指纹，因此升级前留下的部分结果会保留查看，但不会冒险自动续识。

# 0.8.1

- 修正 Windows 模型本地路径测试：比较目录身份，兼容 `runneradmin` / `RUNNER~1` 长短路径，不把同一目录误判为不同路径。
- 将 NumPy、Pillow（含图片格式插件）、PDFium 读取依赖的初始化纳入主线程准备阶段；图片解码仍在后台执行，并在实际读取前回传页码、检查取消。
- CI 线程堆栈诊断覆盖模型准备和文档识别，输出间隔小于请求超时；结束或报错后撤销诊断定时器，正常软件运行不启用该诊断。
- 修复窄窗口/高缩放下图片预览被操作栏挤成零高度：为图片保留最小高度，小窗口中间栏允许滚动；标准版与 WebKitGTK 4.0 兼容版同步生效。
- 增加真实子进程识别超时堆栈、解码初始化顺序、取消前不读取图片、退出专注模式后尺寸恢复的回归测试；布局检查覆盖四种旋转角度和窄/宽窗口切换，失败时保存截图和元素尺寸。
- 不新增功能、不升级 Paddle 依赖、不修改 Debian 10 / WebKitGTK 4.0 / glibc 2.28 构建基线；兼容 0.8.0 的模型包与已有会话。

验证边界：0.8.0 Windows 日志没有线程堆栈，不能仅凭它认定具体原生死锁原因。本版调整初始化顺序并补充诊断，实际 Windows 冻结包是否通过须重跑 GitHub Actions；本地单元测试不等于目标系统安装包验收。

# 0.8.0

- PDF 支持指定实际页码、范围校验、按所选页面识别和进度显示；不合并跳过页码的非连续表格。
- 支持图片左转、右转、还原及批量应用旋转角度；遵循 EXIF 方向，不修改原文件。
- 支持离线模型包准备/导出/导入、SHA-256 校验、独立进程试识别、取消及导入失败回滚；可切换为仅使用本地模型。
- Windows WebView2 缺失时提供引导，保留精简安装包；沿用 0.7.5 图标与全部既有识别功能。

# 0.7.5

- 正式采用灰绿色透明纸张图标：灰绿配色、修长纸张、浅色大折角、三条文字横线和两枚扫描角。
- 统一更新 SVG 源图、PNG 各尺寸、Windows ICO 和 ICNS；标准版与 WebKitGTK 4.0 兼容版共用同一套资源。
- 新增 `npm run icons:generate`，从 SVG 同步生成平台图标，补充图标资源与透明通道回归检查。
- 仅更新图标、版本号和相关文档，不更改 OCR、模型、界面布局和构建工作流。

# 0.7.4

- 移除单模型“重下”和缺失模型修复入口、协议及隐藏备份逻辑，统一采用删除当前组合缓存后重新准备。
- 模型列表改为名称、状态、大小三列；大小使用等宽数字并右对齐。
- 模型路径在 Windows 优先使用 Cascadia Mono/Consolas，中文回退到无衬线字体。
- 自动保存状态移到右上角，区分读取、已启用、保存中、已保存和失败；左下角只显示识别进程状态。

# 0.7.3

- 修复 Windows 源码测试误将文本流默认 CRLF 当成错误的问题；不改变应用输出行为。
- 同一输出流测试覆盖平台默认换行、显式 LF 和显式 CRLF，保留即时刷新、底层 `.buffer` 与进度事件断言。
- 区分本次日志中的 Windows 测试失败和 Linux 打包被取消；不根据取消日志修改 Paddle 依赖或超时。

验证边界：本地单元测试不等于 Windows 冻结包验证。0.7.2 日志尚未进入模型准备测试，早前首次 prepare 超时是否解决仍需重跑 Actions 确认。

# 0.7.2

- 纠正 0.7.1 的超时推断：最新日志卡在第一次轻量模型准备，不是连续切换模型。
- Paddle/PaddleOCR/PaddleX 首次导入在主线程完成，随后模型创建/下载仍在后台进行；导入期间暂停前端缓存轮询。
- 修正 ModelProgressStream 的内部文本缓存遮蔽标准 `.buffer` 接口，并及时刷新 stderr。
- 增加依赖导入、流水线创建阶段日志和 CI 等待心跳；仅冒烟测试启用每分钟 Python 线程栈输出。
- 超时清理整个测试 sidecar 进程树；补充线程顺序、输出流、超时、真实管道（模型桩）回归测试。

验证边界：此次修订未在本地 Windows 冻结环境复现并确认原生卡死已消失，需 GitHub Actions 重跑确认。保持既有 Paddle 依赖和模型缓存不变。

# 0.7.1

- Windows 切换模型组合时重启 sidecar，隔离 Paddle 原生 predictor 的进程状态。
- 冻结包冒烟测试按轻量文字、高精度文字、轻量表格分别启动独立进程。
- 冒烟测试会打印当前场景、请求和最近进度；超时后明确报告卡住位置和近期 stderr，并清理子进程，不再只显示 `_queue.Empty`。

# 0.7.0

- 本机任务/结果/校对和设置自动保存；恢复不自动运行，缺失原文件有提示。
- 恢复或拖放图片时逐文件授权预览；预览失败独立提示。
- PDF 逐页结果保存，后续页面追加不抢占选择、不丢失已校对文字或滚动位置。
- 拖放、多选、排序、批量重试、跳过当前文件；未导出结果关闭提醒。
- TXT/XLSX/HTML 任意组合；分别或汇总导出；文件名模板和同名处理；导出前预览。
- Excel 公式样式的识别文字作为字符串写入，避免公式执行。
- 后台模型准备、已落盘大小/完整性提示、保留备份的单模型重下/缺失模型修复。
- 脱敏日志、诊断包、识别进程重启与迟到退出事件隔离。
- GitHub Actions 增加安装包启动握手、x64 检查和分辨率/缩放布局测试。
- 没有新增快捷键；保留已有 Esc 退出专注模式。

边界：未完成 PDF 重试从头开始；自动保存不加密；模型完整性是必要文件检查而非哈希校验；未知下载总量使用不定进度。新 CI 步骤需要实际运行成功后才能确认目标平台安装包通过验证。
